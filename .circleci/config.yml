version: 2
jobs:
  build:
    docker:
      - image: debian:stretch
    steps:
      - checkout
      - run:
          name: update-npm 
          command: 'sudo npm install -g npm@latest'
      - run:
          name: install-npm
          command: npm install

  #Þarf að run puppiter test líka ef við náum því  
  # push to heroku https://circleci.com/integrations/heroku/



